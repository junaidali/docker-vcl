version:  "3"
services:

  db:
    image: mariadb:10
    volumes:
      - mariadb_data:/var/lib/mysql
    env_file: .env

  www:
    build: www
    env_file: .env
    volumes:
      - /var/log/apache2
    ports:
      - "80:80"
      - "443:443"
    links:
      - db:db

  managementnode:
    build: mgmt
    env_file: .env


volumes:
  mariadb_data: